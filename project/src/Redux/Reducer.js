
const initialState = {
    // Your initial state
    loading : false,
    currentUser : null,
     console: null,
};

const userReducer = (state = initialState, action) => {
    switch (action.type) {
        
        default:
            return state;
    }
};

export default userReducer;
